import { Text, View } from "react-native";

const StoryReflection = () => {
  return (
    <View className="items-center mt-5 mb-5 px-10">
      <Text className="text-center text-2xl font-light leading-relaxed">
        And this is only the beginning.
      </Text>

      <Text className="text-mutedForeground text-center mt-4 italic">
        Your story keeps writing itself — every day. ✨
      </Text>
    </View>
  );
};

export default StoryReflection;
